# Optimization Log

## 2026-06-07 Codex 收尾修复

### 已完成
- 全量 JS 语法检查：12 个 JS 文件通过。
- WXML 事件绑定静态检查：通过。
- `git diff --check`：仅 LF/CRLF 换行提示，无实际 diff 错误。
- 编辑器页面角色修复：`bookPages` 增加 `role`，同步 `isCoverPage`。
- 封面页保护：隐藏删除按钮，模板应用和删除逻辑阻止封面页。
- 提取到手帐链路：`putInJournal()` 传递 `stickerId`，编辑器初始化后自动放置贴纸且只执行一次。
- 矩形文字功能收口：更多菜单动态显示添加/编辑文字，锁定状态阻止修改。
- 手写字体加载：多源兜底，加载失败不影响小程序启动。
- 图片/矩形更多菜单：从 `wx.showActionSheet` 改为自定义底部菜单，规避原生弹窗上限和平台差异。
- 自定义更多菜单支持：图片效果切换、矩形添加/编辑文字、矩形添加图片、复制/粘贴样式、边框、置顶/置底。
- 模板占位文字继续收口：清单模板的待办文字和底部统计已移入矩形元素自身。
- 纸面材质补丁：编辑器 `_isLightColor()` 现在支持 `rgb/rgba/#rgb/#rrggbb`，避免浅色透明背景被误判为暗色纸面。
- 纸面材质 UI 预览增强：背景面板的纸纹/画布/牛皮纸/亚麻/水彩预览改为多层 CSS 纹理，和 Canvas 端真实纸纹方向保持一致。

### 待完成
- 在微信开发者工具中做完整手动回归。
- 继续优化纸面材质的视觉强度和真实感。
- 继续打磨 alpha-mask 贴纸描边效果，尤其是复杂透明边缘、细发丝边缘。
- 梳理 `editor.js` 大文件，后续建议拆分渲染、元素操作、页面导航、面板状态四类模块。

### Git 状态
- 当前分支：`feature/editor-enhancements`。
- 本轮未提交。
- 工作区存在其他程序员/历史改动：包括多个页面和工具文件修改、一个本地字体文件删除、`.workbuddy/memory/MEMORY.md` 未跟踪。

## 2026-06-10 知识库 QA 与作者文档整理

### 已完成
- [x] 阅读项目资料：`设计.md`、产品升级方案、最小成功案例技术文档、近期改动、v1.0.1 更新日志、备案说明、既有 memory。
- [x] 对照当前页面与工具实现：首页、编辑器、素材库、保存收藏物、主题系统、模板系统、本地存储。
- [x] 新增 `docs/知识库QA问题整理.md`，整理 30 条可用于知识库 QA 的用户疑问与回答。
- [x] 新增 `docs/关于作者.md`，整理作者小晴、创作初衷、审美方向、产品原则和 QA 关键词。
- [x] 在 `codex/memory/project-memory.md` 记录本轮知识库整理结果和当前口径。

### 待完成
- [ ] 若后续重新启用 MiMo/remove.bg，需要同步更新 QA 文档中的「当前实现」和「隐私/上传」口径。
- [ ] 若产品正式名称从「月照雪」切换为「我的手帐」，需要检查备案说明、项目配置、页面标题是否完全一致。
- [ ] AI QA 对接前建议把问答按「基础定位/编辑器/素材库/导出隐私」切分入库，并设置当前实现优先级。

### Git 提交记录
- `6f6a07d` - `fix: 补强编辑页工具栏 hover 状态`，已推送到 `origin/dev`。

## 2026-06-16 v1.0.1 正式发布、分支整理与计划性更新调研

### 已完成
- [x] 读取项目根目录 `memory/optimization-log.md` 与 `codex/memory/*`，确认当前项目记忆、待验收事项和历史优化记录。
- [x] 确认仓库远端为 `git@github.com:yuanyue1234/Moonlit-Log.git`，当前本地 `master` 跟踪 `origin/main`。
- [x] 确认旧本地分支 `feature/editor-enhancements` 已并入当前主线，无 `master..feature/editor-enhancements` 未合并提交。
- [x] 调研同类手帐/日记/网页与小程序方向：时光手帐、微手帐、Goodnotes、Zinnia、Day One、Journey、Diarium、Grid Diary/格志日记、Apple Journal、LemonJournal、Canva。
- [x] 新增 `docs/计划性更新路线图.md`，整理 v1.0.2-v1.5 推荐节奏和待决策更新菜单。
- [x] 更新 `codex/memory/project-memory.md`，记录本次发布目标、分支口径和后续计划方向。

### 待完成
- [x] 将本地 `master` 规范为 `main`。
- [x] 清理旧本地分支 `feature/editor-enhancements`。
- [x] 提交本轮文档与 memory 更新，创建 `v1.0.1` tag 并推送到 GitHub。
- [x] 从正式版创建并推送 `dev` 分支，后续计划性更新从 `dev` 开始。
- [ ] 因本机未安装 GitHub CLI `gh`，本轮不创建 GitHub PR；如后续需要 PR，需要先安装并登录 `gh`。
- [ ] v1.0.2 前仍需在微信开发者工具中做完整手动回归。

### Git 提交记录
- `f4cd0c0` - `v1.0.1: 正式发布手帐文档与更新路线图`，已推送到 `origin/main`。
- `v1.0.1` - 正式版本 tag，已推送到 GitHub。
- `dev` - 后续冒险分支，已推送并跟踪 `origin/dev`。

## 2026-06-16 dev 线条风视觉与贴纸绘制功能

### 已完成
- [x] 使用 `redesign-existing-projects` 技能审计当前小程序视觉：原界面以暗色背景和高饱和绿色为主，不符合“线条风、可爱 icon、柔和颜色”的新方向。
- [x] 更新全局主题与小程序导航/Tab 配色：`app.wxss`、`app.json` 转为暖纸色、粉色强调、柔和边线和手帐字体栈。
- [x] 为首页、编辑器、素材库、提取页、模板页追加柔和线条风覆盖样式，保持原页面结构不变。
- [x] 新增 `pencil-line.svg`、`photo-frame.svg` 两个线条图标。
- [x] 编辑器新增“手绘”工具与绘制面板：支持选择线条颜色、粗细、清空、保存为贴纸，保存后进入“手绘”分组并放入当前画布。
- [x] 新增照片自动入素材库逻辑：普通相册/拍照添加的图片、形状填充图片、页面打开时已有的图片/填充图片都会按路径去重后同步到素材库。
- [x] 新增 `photo-frame` 图片效果：普通新增照片默认带相框；图片更多菜单、提取页后处理选项、素材库样式、编辑器 Canvas、PDF 绘制均已接入。
- [x] `utils/file.js` 清理引用补充 `fillImage`，保护形状内照片文件。

### 验证
- [x] `node --check`：所有 JS 文件通过。
- [x] `git diff --check`：仅 LF/CRLF 换行提示，无尾随空格或 diff 格式错误。

### 待完成
- [ ] 微信开发者工具运行回归：确认首页、素材库、提取页、模板页在真机预览中的柔和线条风效果。
- [ ] 编辑器运行回归：确认“手绘”画布坐标、保存为贴纸、自动放入画布、素材库“手绘”分组刷新正常。
- [ ] 照片链路运行回归：确认相册/拍照新增照片默认相框、自动进入“照片”分组；形状填图后自动入库且形状内照片不丢失。
- [ ] 导出回归：确认单页图片和多页 PDF 中 `photo-frame` 视觉一致。

### Git 提交记录
- `feat: 线条风界面与手绘贴纸` - 本轮功能提交至 `dev`，包含线条风视觉、手绘贴纸、照片自动入库、相框效果与 memory 更新。

## 2026-06-16 dev UI 统一与全屏手绘修复

### 已完成
- [x] 删除末尾旧暗色覆盖块，并在 `pages/editor/editor.wxss` 末尾增加渲染顺序保护，确保编辑页背景、顶部栏、页码栏、工具栏、弹层和按钮保持暖纸色线条风。
- [x] 统一需要记住的风格组件：编辑页背景、画布外壳、浮动工具栏、选中工具栏、所有底部弹层、选中更多菜单、颜色选择器、形状填图弹窗、颜色圆点、自定义颜色按钮、TabBar PNG、SVG 图标、全屏手绘工作台。
- [x] 将 `assets/icons` 中 SVG 的 `currentColor` 改为固定暖棕色，新增 `eraser.svg`，避免微信小程序 `<image>` 图标渲染为空或不明显。
- [x] 重绘 `images/tab-*.png`：手帐/素材库/提取备用图标改为清晰线条图标，未选中暖棕、选中玫瑰粉。
- [x] 将手绘功能从底部小面板改为全屏工作台：顶部关闭/完成，下方四项工具栏为选择贴纸、画笔颜色、画笔粗细、工具，右下方悬浮撤回/重做。
- [x] 手绘增加笔画历史：支持撤回、重做、画笔/橡皮切换、颜色弹层、粗细弹层、素材贴纸盖章；保存时按内容边界裁剪，减少透明白边。
- [x] 自定义颜色按钮加大中心 `+` 图标并收窄白边观感，让颜色入口更清楚。

### 验证
- [x] `node --check`：所有 JS 文件通过。
- [x] JSON 解析检查：所有 `.json` 文件通过。
- [x] 编辑器 WXML 静态资源路径检查：37 个静态资源引用均存在。
- [x] `git diff --check`：仅 LF/CRLF 换行提示，无尾随空格或 diff 格式错误。
- [x] 微信开发者工具内置编译器验证：所有 `.wxml` 通过 `wcc` 编译；所有 `.wxss` 通过 `wcsc -lc` 编译。
- [x] 编辑器 `editor.wxss` 编译产物尾部确认包含暖纸色 render-order guard，可覆盖历史暗色规则。
- [x] 已定位本机微信开发者工具 CLI：`D:\Program Files (x86)\Tencent\微信web开发者工具\cli.bat`；`preview/open/auto/quit` 命令可启动工具但本轮调用未在超时内返回，未获得二维码或 GUI 截图。

### 待完成
- [ ] 微信开发者工具运行回归：确认编辑页背景、顶部栏、页码栏、工具栏和所有底部弹层都保持暖纸线条风。
- [ ] 手绘运行回归：确认全屏画布坐标、颜色/粗细弹层、橡皮切换、撤回/重做、选择贴纸盖章和完成保存均可用。
- [ ] 真机/预览回归：确认 TabBar PNG 图标、编辑页 SVG 图标和 hover/active 状态在移动端足够清楚。

### Git 提交记录
- `6dcb021` - `fix: 修复编辑页线条风与全屏手绘工作台`，已推送到 `origin/dev`。
- `cd9b347` - `docs: 记录编辑页编译验证结果`，已推送到 `origin/dev`。

## 2026-06-17 dev 工具栏 hover/触摸态补强

### 已完成
- [x] 为编辑页顶部图标按钮、模式切换、选中工具栏、页码按钮、浮动工具栏、选中更多菜单、素材卡片、弹层关闭按钮和全屏手绘工具按钮增加 `hover-class="soft-hover"`。
- [x] 为画笔颜色圆点增加 `hover-class="soft-hover-scale"`，让颜色选择反馈更明确。
- [x] 在 `pages/editor/editor.wxss` 末尾增加 `.soft-hover` / `.soft-hover-scale`：统一粉色 hover 背景、玫瑰色文字和图标、轻微放大；激活态 hover 仍保持白色图标和文字。

### 验证
- [x] `node --check`：所有 JS 文件通过。
- [x] 微信开发者工具内置 `wcc`：所有 WXML 文件通过编译。
- [x] 微信开发者工具内置 `wcsc -lc`：所有 WXSS 文件通过编译。
- [x] 编辑器静态图标资源检查：43 个 `/assets/icons/*.svg` 或 `/images/*.png` 引用均存在。
- [x] SVG 图标检查：`assets/icons` 中已无 `currentColor`。
- [x] `git diff --check`：仅 LF/CRLF 换行提示，无尾随空格或 diff 格式错误。
- [x] GUI 补充验证：微信开发者工具中项目可启动到首页，暖纸色页面和底栏书本图标可见。
- [x] 清理验证残留：按命令行精确停止此前 CLI 超时留下的 `--port 9421` node 进程，确认 3799-3803/9421 临时端口不再监听。

### 待完成
- [ ] 微信开发者工具 GUI 或真机里确认 hover/触摸态是否与参考图一致。
- [ ] 手绘工具栏在真机上确认弹层不遮挡绘画区域、撤回/重做按钮位置顺手。
- [ ] 若要获得自动化预览二维码/运行截图，需要先关闭当前已打开且无 CLI 控制口的微信开发者工具实例，再用指定 `--port` 重新启动。

### Git 提交记录
- `6f6a07d` - `fix: 补强编辑页工具栏 hover 状态`，已推送到 `origin/dev`。
- `adbccdd` - `docs: 更新 hover 状态提交记录`，已推送到 `origin/dev`。

## 2026-06-17 dev 贴纸唯一性、删页同步、手绘画布和裁切入口

### 已完成
- [x] `utils/storage.js` 增加贴纸引用扫描、安全文件删除和 `deleteStickersForElements()`，删页、删本、删元素、清空页面时会同步回收页面独有贴纸记录。
- [x] `saveSticker()` 扩展归属字段：`ownerBookId`、`ownerPageId`、`ownerElementId`、`linkedPageId`、`linkedElementId`、`uniqueAsset`，用于跟踪“图片/手绘贴纸属于哪一页哪一个元素”。
- [x] 新增图片、形状填图、手绘贴纸、裁切图片都会写入唯一贴纸归属；形状换图和图片裁切会回收旧贴纸。
- [x] 删除封面页图片时同步清理书本 `coverImage`，避免再次点击手账本时旧封面图被封面生成逻辑带回或位置漂移。
- [x] 底部页码跳转条改为仅预览模式显示；编辑模式下添加下一页和删除此页移动到右上角更多菜单，解决第一页/非第一页按钮位置不一致。
- [x] 全屏手绘画布以 `.draw-board` 实际尺寸初始化，canvas 绝对铺满绘画区域，并修掉旧 `height: 360rpx` 样式覆盖。
- [x] 手绘撤销/重做按钮移动到顶部“完成”旁边，保留统一线条风按钮样式。
- [x] 图片更多菜单新增“裁切”，调用 `wx.cropImage` 后替换当前图片、生成新的唯一贴纸并清理旧贴纸；锁定图片不可裁切。

### 验证
- [x] `node --check pages/editor/editor.js` 通过。
- [x] `node --check utils/storage.js` 通过。
- [x] storage mock 验证：新增图片贴纸入库后删除对应页面，贴纸库记录同步从 1 清零。
- [x] 微信开发者工具内置 `wcc`：当前所有 WXML 文件通过编译。
- [x] 微信开发者工具内置 `wcsc -lc`：当前所有 WXSS 文件通过编译。
- [x] 编辑器静态资源检查：`pages/editor/editor.wxml` 中 35 个直接本地 `/assets` 或 `/images` 引用均存在。
- [x] JSON 解析检查：当前 9 个 `.json` 文件均可解析。
- [x] `git diff --check`：仅 LF/CRLF 换行提示，无尾随空白或 diff 格式错误。
- [x] 项目无 `package.json`，本轮无 npm 构建/测试脚本可运行。

### 待完成
- [ ] 微信开发者工具 GUI/真机回归：新增图片后删页，确认“照片/手绘/形状照片”贴纸库无残留。
- [ ] GUI/真机回归：删除封面图片后返回首页再点击手账本，确认旧封面不会重现。
- [ ] GUI/真机回归：预览态底部页码可翻页，编辑态底部页码隐藏，更多菜单可添加/删除页面。
- [ ] GUI/真机回归：手绘画布下半部分可绘制，撤销/重做位于“完成”旁且可用。
- [ ] GUI/真机回归：图片更多菜单裁切入口可用；当前实现默认 `cropScale: '1:1'`，后续可扩展裁切比例选择。

### Git 提交记录
- `ca98316` - `fix: 同步贴纸资产与手绘裁切交互`，已推送到 `origin/dev`。
- `4b077ed` - `docs: 更新贴纸同步提交记录`，已推送到 `origin/dev`。
