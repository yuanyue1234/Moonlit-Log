# 手帐小程序项目记忆

## 当前项目状态
- 微信小程序，核心页面包括首页、编辑器、贴纸库、素材提取、模板选择。
- 编辑器使用 Canvas 2D 自绘，元素包含文字、图片、贴纸、矩形/装饰、主题固定元素。
- 数据存储在本地 storage：`journal_books`、`journal_pages`、`sticker_assets`。
- 手帐体系已调整为：每本手账必须有第一页封面，封面包含名称、标签、主题风格；普通页按主题生成不同背景、纸面材质和固定装饰元素。

## 本轮关键修复
- 修复编辑器编译失败后的遗留风险：全量 JS 语法检查通过。
- 修复封面保护链路：`bookPages` 现在包含 `role`，封面页不会被删除或套用模板。
- 修复提取页“放入手帐”只跳转不放入素材的问题：现在会带 `stickerId` 进入编辑器，并自动放置一次。
- 修复矩形文字入口体验：矩形更多菜单根据状态显示“添加文字/编辑文字”，并防止锁定元素被文字面板修改。
- 字体加载改为多源兜底：优先使用 LXGW Yozai GitHub release，失败后尝试备用源，全部失败则降级系统字体且不阻断启动。
- 贴纸后处理仍采用 alpha mask 方向：白边、纸贴、阴影贴合透明主体轮廓，避免矩形底。

## 仍需真实设备/开发者工具验证
- 微信开发者工具中进入首页，点击手账本进入编辑器。
- 选择图片元素后点更多菜单，确认 `无效果/白边/纸贴/阴影/复制样式/粘贴样式` 可用。
- 矩形点更多菜单，确认可添加/编辑文字，图片可覆盖矩形占位文本。
- 提取页完成后点“放入手帐”，确认编辑器打开后素材自动出现在画布。
- 预览模式翻页和封面页删除/套模板保护需要运行时确认。

## 2026-06-10 知识库文档整理
- 新增 `docs/知识库QA问题整理.md`：面向后续 AI QA，对用户可能疑惑的问题整理为 30 条问答，覆盖项目定位、首页、编辑器、模板、素材库、导出、隐私、AI/remove.bg 历史方案与当前口径。
- 新增 `docs/关于作者.md`：整理作者小晴的项目身份、创作初衷、审美方向、产品原则和后续 QA 回答关键词。
- 当前文档口径：公开/备案默认强调本地保存、不上传用户内容、不请求位置权限；仓库中保留的 MiMo/remove.bg 文档视为历史方案或后续可选扩展，QA 回答需避免与当前实现混淆。

## 2026-06-16 v1.0.1 正式发布与后续计划
- 当前发布目标：将现有手帐项目作为 `v1.0.1` 正式版本推送到 GitHub，并建立 `dev` 分支作为后续计划性更新入口。
- 分支整理口径：远端主线为 `origin/main`；本地旧分支 `feature/editor-enhancements` 已完全并入当前主线，可清理；本地 `master` 计划规范为 `main`。
- 新增 `docs/计划性更新路线图.md`：调研时光手帐、微手帐、Goodnotes、Zinnia、Day One、Journey、Diarium、Grid Diary/格志日记、Apple Journal、LemonJournal 小程序 demo、Canva 等产品后，整理 v1.0.2-v1.5 的计划性更新节奏。
- 后续产品方向：保持本地优先和隐私优先；近期优先做“今日一页入口、提示卡、心情标签、最近使用、JSON 备份导出、手动回归清单”，暂缓云同步、社区、AI 抠图等高风险能力。
- 发布完成状态：`main` 已推送到 GitHub，`v1.0.1` tag 指向正式发布提交 `f4cd0c0`；`dev` 已从该正式版本切出并推送，作为后续更新分支。本机未安装 GitHub CLI `gh`，因此本轮未创建 PR。

## 2026-06-16 dev 线条风界面与贴纸绘制
- 设计方向：在不改变现有首页、编辑器、素材库、提取页、模板页信息架构的前提下，从黑底荧光绿工具风转为柔和暖纸色、粉色强调、线条边框和手帐字体气质。
- 新增 `assets/icons/pencil-line.svg` 和 `assets/icons/photo-frame.svg`，用于手绘贴纸与相框效果入口。
- 编辑器新增“手绘”工具：打开底部绘制面板，在 Canvas 里自由画线，可选择颜色和粗细，保存后写入素材库“手绘”分组并自动放入当前画布。
- 普通相册/拍照添加进手帐的照片现在会自动写入素材库“照片”分组；通过矩形、圆形、心形、星形等形状填充的照片也会自动入库。
- 新增图片效果 `photo-frame`：普通新增照片默认使用柔和相纸边框；图片更多菜单、提取页后处理、素材库预览、单页导出和 PDF 绘制都识别该效果。
- 文件清理逻辑补充识别 `fillImage`，避免形状内照片被误判为未引用文件。

## 2026-06-16 dev UI 统一与全屏手绘修复
- 本轮修复目标：统一编辑手帐页的暖纸色线条风，修掉旧暗色 CSS 覆盖导致的背景、弹层、按钮、文字和图标状态不一致。
- 风格组件清单后续必须一起维护：编辑页背景、`canvas-wrapper`、`page-shell`、顶部栏、页码栏、浮动工具栏、选中工具栏、素材/文字/矩形/背景/模板/边框底部面板、选中更多菜单、颜色选择器、形状填图拖拽弹窗、颜色圆点/自定义颜色按钮、TabBar PNG 图标、`assets/icons` SVG 图标、全屏手绘工作台。
- SVG 图标不再依赖 `currentColor`，统一写入暖棕色描边/填充，避免微信小程序 `<image>` 渲染时图标不可见；激活态由 WXSS filter 转为白色。
- TabBar 的 `images/tab-*.png` 已重绘为清晰线条图标：手帐为书本，素材库为贴纸，保留提取图标资源备用；未选中暖棕，选中玫瑰粉。
- 手绘界面从底部小面板改为全屏工作台：顶部关闭/完成，下方工具栏为“选择贴纸、画笔颜色、画笔粗细、工具”，右下方悬浮撤回/重做，画布区域大幅增大。
- 手绘历史现在按笔画记录，支持撤回/重做；工具按钮可在画笔和橡皮之间切换；保存时按可见内容裁剪，减少透明白边。
- 本机微信开发者工具 CLI 路径为 `D:\Program Files (x86)\Tencent\微信web开发者工具\cli.bat`。本轮 `wcc`/`wcsc` 编译通过所有 WXML/WXSS；但 `preview/open/auto/quit` 在 CLI 超时内未返回，因此仍需人工在 GUI/真机确认视觉与交互。

## 2026-06-17 dev 工具栏 hover/触摸态补强
- 为编辑页主工具栏、顶部图标按钮、选中工具栏、页码按钮、选中更多菜单、素材/贴纸入口、弹层关闭按钮和全屏手绘工作台增加微信小程序原生 `hover-class="soft-hover"`。
- 新增 `.soft-hover` 和 `.soft-hover-scale` 作为统一交互态：柔和粉色底、玫瑰色文字/图标、轻微放大，激活按钮 hover 时保持白色图标与文字。
- 这次补强针对“工具栏文字颜色、hover 颜色、移动上去的 icon 颜色不直观”的问题，避免只依赖普通 CSS `:hover/:active`。
- GUI 验证补记：微信开发者工具 Stable v2.01.2510290 中项目可启动到首页，暖纸色背景和底栏书本图标可见；此前 CLI `preview/open/auto/quit` 残留的 `--port 9421` node 进程已按精确命令行清理。
- 当前仍缺完整运行证据：开发者工具实例已提前打开且无可用 CLI 自动化端口，模拟器在当前桌面布局中被右侧裁切，未能可靠进入编辑页/全屏手绘工作台截图；后续需要关闭并用 CLI 端口重启开发者工具，或由人工在 GUI/真机完成编辑页、弹层、手绘工具栏回归。

## 2026-06-17 dev 贴纸唯一性、删页同步、预览页码和图片裁切
- 贴纸资产新增归属字段：`ownerBookId`、`ownerPageId`、`ownerElementId`、`linkedPageId`、`linkedElementId`、`uniqueAsset`；新增图片、形状填图、手绘贴纸、裁切后的图片都会写入当前页面/元素归属。
- `utils/storage.js` 新增页面元素引用扫描与安全删除能力：删单页、删整本书、删除单个元素或清空页面时，会同步清理该页面独有的 `journal-photo`、`shape-photo`、`drawn-sticker` 贴纸记录，并在文件仍被其他页面、书本封面或贴纸记录引用时保留本地文件。
- 封面图删除链路新增 `clearBookCoverImage()`：删除封面页上的封面图片时先清掉书本 `coverImage` 元数据，再回收贴纸，防止再次点击手账本时旧封面图被系统封面重建逻辑带回来或位置漂移。
- 历史实现曾将底部页码跳转条改为仅预览显示、编辑态加删页移入更多菜单；该方案已在 2026-06-17/18 后续需求中废弃，当前口径见文末“编辑/预览布局二次修正”。
- 全屏手绘工作台修正可绘制区域：画布初始化以 `.draw-board` 的真实矩形尺寸为准，`.draw-board .draw-sticker-canvas` 在最终 WXSS guard 中绝对铺满容器；旧的 `height: 360rpx` 规则已限定到废弃的 `.draw-canvas-card`。
- 手绘撤销/重做按钮从画布右下悬浮区移到顶部“完成”按钮旁边，保留与编辑页图标按钮一致的线条风和 hover 状态。
- 图片更多菜单新增“裁切”入口，使用 `wx.cropImage` 成功后持久化裁切图、替换当前图片元素、生成新的唯一贴纸，并回收旧贴纸；锁定图片会阻止裁切。
- 本轮验证：`node --check pages/editor/editor.js`、`node --check utils/storage.js` 通过；storage mock 验证“图片贴纸入库后删除页面，贴纸库记录同步清零”通过；微信开发者工具内置 `wcc`/`wcsc -lc` 通过当前所有 WXML/WXSS；编辑页 35 个直接本地静态资源引用均存在；9 个 JSON 文件均可解析；`git diff --check` 仅提示 LF/CRLF 换行，无尾随空白错误。项目没有 `package.json`，本轮无 npm 构建脚本可运行；微信开发者工具 CLI 运行验证仍需在可控 CLI 端口/GUI 真机中补做。

## 2026-06-17 dev 编辑页 UI/手绘护栏脚本
- 新增 `scripts/verify-editor-ui.js`，将本轮目标固化为 41 项静态护栏：TabBar 暖色与图标文件、SVG 无 `currentColor`、编辑页本地资源存在、soft-hover 覆盖、自定义颜色 `+` 放大、预览态页码条、手绘顶部撤销/重做/完成、底部四工具顺序、画布铺满、裁切入口、贴纸唯一字段和删页/封面清理链路。
- 当前脚本运行结果：`node scripts/verify-editor-ui.js` 通过 41 项检查；`node --check scripts/verify-editor-ui.js` 通过。
- 当前仍不能代替 GUI/真机验证：它能防止代码结构和资源回退，但不能证明微信开发者工具模拟器/真机中的实际触摸、裁切弹窗、画布下半区绘制和视觉贴合参考图。

## 2026-06-17 dev 贴纸生命周期脚本与运行回归清单
- 新增 `scripts/verify-sticker-lifecycle.js`，用本地 `wx` mock 验证贴纸生命周期：删除页面会移除页面独有贴纸、孤立文件会被回收、被其他页面继续引用的文件不会被误删、封面 `coverImage` 清空后可回收原封面贴纸。
- 当前脚本运行结果：`node scripts/verify-sticker-lifecycle.js` 通过 9 项检查；`node --check scripts/verify-sticker-lifecycle.js` 通过。
- 新增 `docs/编辑页运行回归清单.md`，记录 GUI/真机必须确认的完整步骤：视觉统一、页码条和页面管理、图片/贴纸/删页、图片裁切、全屏手绘、自动检查命令。
- 正常用户环境下 `cli.bat --help` 可用；但 `cli.bat preview --project ... --port 9421` 等待 180 秒未返回，也未生成预览二维码/信息文件。超时后已精确清理本次 preview 残留的 `cmd/node` 进程，9421 端口无监听。运行时证据仍需 GUI/真机完成。
- 补充尝试 `cli.bat auto --project ... --port 9421 --trust-project --disable-gpu`，等待 60 秒未返回，9421 端口仍无监听；本次 auto 残留的 `cmd/node` 进程已按 PID 精确清理。因此当前自动化入口无法提供真机/模拟器交互证据。

## 2026-06-17 dev v1.1.0 编辑体验整理
- 首页大标题已按用户要求改为“月亮手杖”，副标题不变；本轮版本记录为 `docs/v1.1.0-更新日志.md`。
- 编辑模式底部恢复固定页面管理条：上一页、页码、下一页、添加页、删除页固定排列；没有页码圆点；删除页按钮在封面页/仅剩一页时禁用但占位，避免第一页和第二页按钮位置割裂。
- 此阶段曾把预览模式底部组件全部隐藏；该口径已于 2026-06-18 被用户明确纠正，当前预览必须显示可跳转的小圆点页码组件。
- 图片更多菜单中的“裁切”改为可逆裁切面板：不再使用 `wx.cropImage` 生成新文件，而是复用形状填图拖拽面板，保存 `cropMode: 'cover'`、`cropOffsetX`、`cropOffsetY` 到图片元素；已裁切图片选中后会出现“调整裁切”按钮。
- “复制样式/粘贴样式”已从更多菜单和对应 JS 死代码中移除。
- 手绘工作台底部移除“选择贴纸”，保留画笔颜色、画笔粗细、工具三项；底部工具按钮改为与编辑页一致的暖纸线条风，画布继续按 `.draw-board` 真实尺寸铺满。
- 贴纸库详情预览增加格纹底，避免白色或透明贴纸放大后在白底上异常不可见。
- 模板页改为更紧凑的三列网格，模板卡补充胶带、标题线、照片框、文字线和圆点线索；编辑器模板面板也同步增加微型版式线索。
- 封面图清理更稳：`clearBookCoverImage()` 现在会同步剔除封面页中匹配的系统封面图片元素，避免旧封面从页面元素里复活。
- 自动检查已更新：`scripts/verify-editor-ui.js` 当前通过 49 项检查；`scripts/verify-sticker-lifecycle.js` 当前通过 9 项检查。WXML/WXSS 已用微信开发者工具内置 `wcc`/`wcsc` 编译改动文件通过。

## 2026-06-18 dev v1.1.0 编辑/预览布局二次修正
- 用户明确纠正上一轮口径：预览模式必须保留可跳转页面的小圆点页码组件；编辑模式保留页数、左右翻页、加页和删页，不显示小圆点。
- 编辑画布逻辑尺寸从 `690×920` 调整为 `690×1120` 长页；新增 `pageDisplayWidth/pageDisplayHeight` 和 `_fitCanvasToViewport()`，按编辑/预览模式的实际剩余空间等比缩放，不通过非等比拉伸填满窗口。
- 预览小圆点使用 `onTapPageDot` 原地切页，当前页使用玫瑰粉高亮；预览组件不包含任何加页/删页编辑操作。
- 编辑工具面板由窗口底边改为停靠在底部主工具栏上方，面板遮罩只覆盖画布区域；主工具栏和页面管理条层级更高，可在面板打开时继续切换工具。
- `scripts/verify-editor-ui.js` 已提升到 57 项检查；新增 `scripts/verify-editor-layout.js`，4 组常见视口的等比和空间占用检查通过。
- 微信开发者工具窗口可由 CLI 拉起，但 CLI 调用持续不返回；当前会话的 Windows 界面控制入口不可用，因此真实截图/触摸仍保留为 GUI/真机回归项。
- 用户于 2026-06-18 明确要求：后续禁止再调用微信开发者工具的 `open`、`preview`、`auto` 或其他命令。后续只使用普通 Node 静态/布局测试和用户手动界面反馈，不再重试该 CLI 路径。

