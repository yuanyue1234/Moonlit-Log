# 手帐小程序项目记忆

## 当前项目状态
- 微信小程序，核心页面包括首页、编辑器、贴纸库、素材提取、模板选择。
- 编辑器使用 Canvas 2D 自绘，元素包含文字、图片、贴纸、矩形/装饰、主题固定元素。
- 数据存储在本地 storage：`journal_books`、`journal_pages`、`sticker_assets`。
- 手帐体系已调整为：每本手账必须有第一页封面，封面包含名称、标签、主题风格；普通页按主题生成不同背景、纸面材质和固定装饰元素。

## 本轮关键修复
- 修复编辑器编译失败后的遗留风险：全量 JS 语法检查通过。
- 修复封面保护链路：`bookPages` 现在包含 `role`，封面页不会被删除或套用模板。
- 修复提取页“放入手帐”只跳转不放入素材的问题：现在会带 `stickerId` 进入编辑器，并自动放置一次。
- 修复矩形文字入口体验：矩形更多菜单根据状态显示“添加文字/编辑文字”，并防止锁定元素被文字面板修改。
- 字体加载改为多源兜底：优先使用 LXGW Yozai GitHub release，失败后尝试备用源，全部失败则降级系统字体且不阻断启动。
- 贴纸后处理仍采用 alpha mask 方向：白边、纸贴、阴影贴合透明主体轮廓，避免矩形底。

## 仍需真实设备/开发者工具验证
- 微信开发者工具中进入首页，点击手账本进入编辑器。
- 选择图片元素后点更多菜单，确认 `无效果/白边/纸贴/阴影/复制样式/粘贴样式` 可用。
- 矩形点更多菜单，确认可添加/编辑文字，图片可覆盖矩形占位文本。
- 提取页完成后点“放入手帐”，确认编辑器打开后素材自动出现在画布。
- 预览模式翻页和封面页删除/套模板保护需要运行时确认。

## 2026-06-10 知识库文档整理
- 新增 `docs/知识库QA问题整理.md`：面向后续 AI QA，对用户可能疑惑的问题整理为 30 条问答，覆盖项目定位、首页、编辑器、模板、素材库、导出、隐私、AI/remove.bg 历史方案与当前口径。
- 新增 `docs/关于作者.md`：整理作者小晴的项目身份、创作初衷、审美方向、产品原则和后续 QA 回答关键词。
- 当前文档口径：公开/备案默认强调本地保存、不上传用户内容、不请求位置权限；仓库中保留的 MiMo/remove.bg 文档视为历史方案或后续可选扩展，QA 回答需避免与当前实现混淆。

## 2026-06-16 v1.0.1 正式发布与后续计划
- 当前发布目标：将现有手帐项目作为 `v1.0.1` 正式版本推送到 GitHub，并建立 `dev` 分支作为后续计划性更新入口。
- 分支整理口径：远端主线为 `origin/main`；本地旧分支 `feature/editor-enhancements` 已完全并入当前主线，可清理；本地 `master` 计划规范为 `main`。
- 新增 `docs/计划性更新路线图.md`：调研时光手帐、微手帐、Goodnotes、Zinnia、Day One、Journey、Diarium、Grid Diary/格志日记、Apple Journal、LemonJournal 小程序 demo、Canva 等产品后，整理 v1.0.2-v1.5 的计划性更新节奏。
- 后续产品方向：保持本地优先和隐私优先；近期优先做“今日一页入口、提示卡、心情标签、最近使用、JSON 备份导出、手动回归清单”，暂缓云同步、社区、AI 抠图等高风险能力。

