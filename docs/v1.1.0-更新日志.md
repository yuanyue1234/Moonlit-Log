# 月亮手杖 v1.1.0 更新日志

日期：2026-06-17
分支：`dev`

补充修正：2026-06-18

## 本次定位

v1.1.0 是一次编辑体验整理：把页面管理、图片裁切、手绘工作台、贴纸预览和模板浏览往“更顺手做完一页手帐”靠近，同时继续守住贴纸唯一性和本地优先。

## 已完成

- 首页主标题从“收藏手帐”改为“月亮手杖”，副标题保持不变。
- 编辑模式底部恢复固定页面管理条：上一页、当前页/总页数、下一页、添加页、删除页固定排列。
- 预览模式显示独立的小圆点页码组件，可点击任意圆点跳转页面；不显示加页、删页等编辑控件。
- 删除页按钮在封面页/第一页或仅剩一页时保持占位但禁用，避免按钮位置跳动。
- 图片“更多”里的裁切改为可逆拖拽裁切：不再调用 `wx.cropImage` 生成新文件，而是保存 `cropMode/cropOffsetX/cropOffsetY` 到图片元素。
- 已裁切图片选中后会出现“调整裁切”按钮，可随时重新拖动位置；撤销/重做可回滚裁切参数。
- 删除封面图时，除了清空书本 `coverImage`，还会同步剔除封面页里的系统封面图片元素，避免旧封面重新出现或位置漂移。
- 手绘工作台底部移除“选择贴纸”，保留画笔颜色、画笔粗细、工具三项，画布下半区继续由真实 `.draw-board` 尺寸铺满。
- 手绘底部工具按钮改为与编辑页一致的暖纸色线条风，避免浅底白字不清楚。
- 贴纸库详情预览加入格纹预览底，白色或透明贴纸放大后也能看清边缘。
- 模板页改为更紧凑的三列浏览，一屏可看到更多模板；模板卡补充胶带、标题线、照片框、文字线等预览线索。
- 编辑器模板面板的模板卡也补充更真实的微型版式线索。
- 移除图片/矩形/元素更多菜单中的“复制样式”和“粘贴样式”，并清掉对应未使用逻辑。
- 编辑画布最终升级为 `690×1200` 长页，并根据设备可用宽高等比缩放；长屏编辑态同时接近填满可用宽度与高度，预览态上下空余进一步收窄，同时不拉伸照片和贴纸。
- 素材、文字、矩形、背景、模板等工具面板改为从主工具栏上方展开；面板与主工具栏固定保留 `12rpx` 间距，遮罩不覆盖底部主工具栏，可直接切换其他工具。
- `scripts/verify-editor-ui.js` 更新为 59 项检查，覆盖编辑页管理条、预览小圆点、长页画布、固定控件尺寸、工具面板层级、可逆裁切、手绘三工具、贴纸预览格纹和模板三列布局。
- 新增 `scripts/verify-editor-layout.js`，对紧凑手机、长屏手机预览/编辑和横向更宽设备共 4 组视口验证画布等比与空间占用。

## 验证

- `node --check pages/editor/editor.js`
- `node --check pages/stickers/stickers.js`
- `node --check utils/storage.js`
- `node scripts/verify-editor-ui.js`：59 项通过。
- `node scripts/verify-editor-layout.js`：4 组视口与 `12rpx` 面板间距检查通过。
- `node scripts/verify-sticker-lifecycle.js`：9 项通过。
- `node --check scripts/verify-editor-ui.js`
- `node --check scripts/verify-sticker-lifecycle.js`
- `git diff --check`：仅 LF/CRLF 换行提示，无尾随空白错误。
- 微信开发者工具内置 `wcc` 编译改动过的 WXML 文件通过。
- 微信开发者工具内置 `wcsc` 编译改动过的 WXSS 文件通过。

## 待人工确认

- 微信开发者工具 GUI/真机中，编辑模式长页画布是否充分占用可用区，底部页面管理条与工具 dock 是否保持清晰间距。
- 预览模式小圆点能否正确显示当前页，并点击跳转到目标页。
- 各工具面板是否稳定在主工具栏上方展开，切换工具时不遮挡或误触。
- 手绘工作台下半区连续绘制、撤销/重做、完成保存是否顺手。
- 图片可逆裁切面板拖动、完成、再次调整、撤销/重做是否符合预期。
- 删除封面图后返回首页再进入，旧封面是否不再复活。
- 添加图片后删除对应页面，贴纸库是否没有页面独有贴纸残留。
